<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Banner with Logo Inside -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginBottom="16dp">

            <!-- Banner background -->
            <View
                android:id="@+id/topBanner"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/bannerGreen" />

            <!-- Logo Text centered inside banner -->
            <TextView
                android:id="@+id/logoText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:fontFamily="@font/logofont"
                android:gravity="center"
                android:shadowColor="#80000000"
                android:shadowDx="4"
                android:shadowDy="4"
                android:shadowRadius="8"
                android:text="@string/b_f_f_2"
                android:textColor="@color/DarkGreen"
                android:textSize="70sp"
                android:textStyle="bold" />
        </FrameLayout>
        <!-- Back Button -->
        <Button
            android:id="@+id/backButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:text="@string/back_goal"
            android:padding="8dp"
            android:backgroundTint="#A58ADF"
            android:textColor="#000000"
            android:layout_marginBottom="12dp"/>

        <!-- Goal Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/new_goal_goal"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"/>

        <!-- Goal Inputs -->
        <EditText
            android:id="@+id/goalNameInput"
            android:hint="@string/goal_name_goal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text" />
        <!-- Goal Description -->
        <EditText
            android:id="@+id/goalDescriptionInput"
            android:hint="@string/description_goal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minLines="3"
            android:gravity="top"
            android:inputType="textMultiLine"
            android:layout_marginTop="12dp"/>
        <!-- Goal Amount -->
        <EditText
            android:id="@+id/goalAmountInput"
            android:hint="@string/saving_goal_amount_goal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberDecimal"
            android:layout_marginTop="12dp"/>

        <!-- Date Inputs -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="12dp"
            android:gravity="center">

            <EditText
                android:id="@+id/goalDateInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:hint="@string/select_target_date_goal"
                android:focusable="false"
                android:clickable="true" />
        </LinearLayout>

        <!-- Buttons -->
        <Button
            android:id="@+id/confirmAddGoalButton"
            android:layout_marginTop="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_confirmGoal"
            android:backgroundTint="#A58ADF"
            android:textColor="#FFFFFF"/>

        <Button
            android:id="@+id/showSuggestionBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/suggested_goal_suggestionGoal"
            android:layout_marginTop="12dp"
            android:backgroundTint="#A58ADF"
            android:textColor="#FFFFFF"/>
    </LinearLayout>
</ScrollView>